# NAP Legal AI Advisor — Environment Variables
# Copy this file to .env and adjust as needed.

# Inference batch size for risk predictor (lower if CUDA out of memory)
BATCH_SIZE=4

# Device for model inference (cpu or cuda)
DEVICE=cpu

# Path to trained model (default: fold_4)
MODEL_PATH=models/nap_legalbert_cv/fold_4/best_model

# Path to processed data directory
DATA_DIR=Data/processed

# Streamlit server port (default: 8501)
# STREAMLIT_SERVER_PORT=8501
